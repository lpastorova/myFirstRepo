let arr = [7, 9, 0, -2, 3, 6];

function first (arr, n) {
    if (n === 0) {
        //console.log(arr[0]);
        return arr[0]    }
}

//console.log(arr[0]);

//first(arr, 0);
console.log(first(arr, 0))